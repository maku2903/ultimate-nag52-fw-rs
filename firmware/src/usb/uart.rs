

pub fn uart_write() {
    //cortex_m::interrupt::free(|cs| {
    //    let mut uart = UART.borrow(cs).borrow_mut();
    //    if let Some(uart) = uart.as_mut() {
    //        let _ = uart.write("Testing!\n".as_bytes());
    //    }
    //});
}

fn uart_read() {

}